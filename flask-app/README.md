# АвтоПрофи - Flask приложение

Полнофункциональное Flask веб-приложение для автошколы с подключением к PostgreSQL.

## Установка и запуск

### 1. Создать виртуальное окружение
```bash
cd flask-app
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

### 2. Установить зависимости
```bash
pip install -r requirements.txt
```

### 3. Запустить приложение
```bash
python app.py
```

Приложение будет доступно по адресу: http://localhost:5000

## Структура проекта

```
flask-app/
├── app.py                 # Основное Flask приложение
├── requirements.txt       # Зависимости Python
├── templates/            # HTML шаблоны
│   ├── base.html         # Базовый шаблон
│   ├── index.html        # Главная страница
│   └── admin.html        # Админ-панель
└── static/              # Статические файлы
    ├── css/
    │   └── style.css     # Стили
    └── js/
        └── main.js       # JavaScript
```

## Возможности

### Для пользователей:
- 📚 Просмотр доступных курсов обучения
- 👨‍🏫 Информация об инструкторах
- 📝 Форма записи на обучение
- ✅ Валидация данных формы

### Для администраторов:
- 🔐 Вход по паролю: `AutoProfi2024!`
- 📊 Просмотр всех заявок
- ➕ Управление курсами (создание, удаление)
- 👥 Управление инструкторами (создание, удаление)

## API эндпоинты

### Заявки
- `GET /api/enrollments` - Получить все заявки
- `POST /api/enrollments` - Создать заявку

### Курсы
- `GET /api/courses` - Получить все курсы
- `POST /api/courses` - Создать курс
- `PUT /api/courses` - Обновить курс
- `DELETE /api/courses?id={id}` - Удалить курс

### Инструкторы
- `GET /api/instructors` - Получить всех инструкторов
- `POST /api/instructors` - Создать инструктора
- `PUT /api/instructors` - Обновить инструктора
- `DELETE /api/instructors?id={id}` - Удалить инструктора

## База данных

Приложение подключается к PostgreSQL базе данных Render:
- Host: dpg-d4bomnbipnbc7390422g-a.oregon-postgres.render.com
- Database: autoprofi
- User: autoprofi_user

### Таблицы:
- `courses` - Курсы обучения
- `instructors` - Инструкторы
- `enrollments` - Заявки на обучение

## Деплой на production

### Gunicorn (рекомендуется)
```bash
gunicorn -w 4 -b 0.0.0.0:8000 app:app
```

### Render / Heroku
Добавить файл `Procfile`:
```
web: gunicorn app:app
```

## Технологии

- **Backend**: Flask 3.0
- **Database**: PostgreSQL (psycopg2)
- **Frontend**: HTML5, CSS3, Vanilla JavaScript
- **Fonts**: Google Fonts (Montserrat, Open Sans)
- **Deployment**: Gunicorn

## Лицензия

© 2024 АвтоПрофи. Все права защищены.
