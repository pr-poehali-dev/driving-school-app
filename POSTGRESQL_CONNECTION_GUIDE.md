# Инструкция по подключению к облачной базе данных PostgreSQL

## Общая информация

Ваш проект АвтоПрофи использует облачную базу данных PostgreSQL, которая размещена на серверах Яндекс.Облако.

**Строка подключения хранится в секрете:** `DATABASE_URL`

Формат строки подключения:
```
postgresql://username:password@host:port/database
```

---

## Способ 1: Подключение через psql (командная строка)

### Шаг 1: Установка psql
- **macOS**: `brew install postgresql`
- **Ubuntu/Debian**: `sudo apt-get install postgresql-client`
- **Windows**: Скачай PostgreSQL с [официального сайта](https://www.postgresql.org/download/windows/)

### Шаг 2: Получи строку подключения
1. Зайди в настройки проекта на **poehali.dev**
2. Скопируй значение секрета `DATABASE_URL`

### Шаг 3: Подключись к базе
```bash
# Замени на свою строку подключения
psql "postgresql://username:password@host:port/database"

# Или используй переменную окружения
export DATABASE_URL="postgresql://username:password@host:port/database"
psql $DATABASE_URL
```

### Полезные команды psql:
```sql
-- Список таблиц
\dt

-- Описание таблицы
\d courses

-- Список всех схем
\dn

-- Выход
\q

-- Просмотр данных
SELECT * FROM courses;
SELECT * FROM enrollments ORDER BY created_at DESC;
```

---

## Способ 2: DBeaver (рекомендуется для новичков)

### Шаг 1: Установи DBeaver
Скачай бесплатную версию: [dbeaver.io/download](https://dbeaver.io/download/)

### Шаг 2: Создай новое подключение
1. Открой DBeaver
2. Нажми **Database** → **New Database Connection**
3. Выбери **PostgreSQL**
4. Нажми **Next**

### Шаг 3: Заполни данные подключения
Преобразуй строку подключения в параметры:

**Пример строки:**
```
postgresql://user123:mypass456@rc1a-abc123.mdb.yandexcloud.net:6432/dbname
```

**Параметры для DBeaver:**
- **Host**: `rc1a-abc123.mdb.yandexcloud.net`
- **Port**: `6432`
- **Database**: `dbname`
- **Username**: `user123`
- **Password**: `mypass456`

### Шаг 4: Настрой SSL (важно!)
1. Перейди на вкладку **SSL**
2. Включи **Use SSL**
3. **SSL mode**: `require` или `verify-full`

### Шаг 5: Тестируй подключение
1. Нажми **Test Connection**
2. Если DBeaver попросит скачать драйвер — согласись
3. Должно появиться "Connected"
4. Нажми **Finish**

### Работа с базой в DBeaver:
- Раскрой дерево подключения → `Databases` → твоя база → `Schemas` → `public` → `Tables`
- ПКМ на таблице → **View Data** — просмотр данных
- ПКМ на базе → **SQL Editor** → **New SQL Script** — выполнение запросов

---

## Способ 3: pgAdmin (продвинутый вариант)

### Шаг 1: Установи pgAdmin
Скачай с [pgadmin.org](https://www.pgadmin.org/download/)

### Шаг 2: Создай новый сервер
1. Открой pgAdmin
2. ПКМ на **Servers** → **Register** → **Server**

### Шаг 3: Вкладка General
- **Name**: АвтоПрофи (любое имя)

### Шаг 4: Вкладка Connection
Из строки `postgresql://user:pass@host:port/db`:
- **Host**: адрес хоста
- **Port**: порт (обычно 6432 для Яндекс.Облака)
- **Maintenance database**: имя базы
- **Username**: имя пользователя
- **Password**: пароль
- ✅ Включи **Save password**

### Шаг 5: Вкладка SSL
- **SSL mode**: `require`

### Шаг 6: Сохрани
Нажми **Save** — подключение готово!

---

## Способ 4: TablePlus (для macOS/Windows)

### Установка
Скачай с [tableplus.com](https://tableplus.com/)

### Подключение
1. Нажми **Create a new connection**
2. Выбери **PostgreSQL**
3. Заполни поля из строки подключения
4. Включи **SSL/TLS**
5. Нажми **Test** → **Connect**

---

## Импорт дампа в облачную базу

После подключения импортируй данные из файла `database_export.sql`:

### Через psql:
```bash
psql $DATABASE_URL < database_export.sql
```

### Через DBeaver:
1. Открой подключение
2. ПКМ на базе → **SQL Editor** → **Open SQL Script**
3. Выбери `database_export.sql`
4. Нажми **Execute SQL Script** (Ctrl+X)

### Через pgAdmin:
1. Открой Query Tool (ПКМ на базе → **Query Tool**)
2. Нажми **Open File** (папка с открытым документом)
3. Выбери `database_export.sql`
4. Нажми **Execute** (F5)

---

## Безопасность

⚠️ **Важно:**
- Никогда не публикуй строку подключения в открытый доступ
- Не коммить `DATABASE_URL` в Git
- Используй переменные окружения для хранения секретов
- При работе в команде создавай отдельные аккаунты с разными правами

---

## Устранение проблем

### Ошибка: "Connection timeout"
- Проверь, что указан правильный хост и порт
- Убедись, что твой IP не заблокирован файрволом
- Проверь, включен ли SSL режим

### Ошибка: "SSL required"
- В настройках подключения включи SSL mode: `require`

### Ошибка: "Authentication failed"
- Проверь правильность username и password
- Убедись, что не скопировались лишние пробелы

### Ошибка: "Database does not exist"
- Проверь название базы в строке подключения
- Возможно, нужно создать базу вручную

---

## Полезные SQL-запросы для АвтоПрофи

```sql
-- Посмотреть все курсы
SELECT * FROM courses ORDER BY price;

-- Посмотреть последние заявки
SELECT 
    e.id,
    e.full_name,
    e.phone,
    c.title as course_name,
    e.created_at
FROM enrollments e
LEFT JOIN courses c ON e.course_id = c.id
ORDER BY e.created_at DESC
LIMIT 10;

-- Статистика по курсам
SELECT 
    c.title,
    COUNT(e.id) as enrollments_count,
    c.price
FROM courses c
LEFT JOIN enrollments e ON c.id = e.course_id
GROUP BY c.id, c.title, c.price
ORDER BY enrollments_count DESC;

-- Все инструкторы с рейтингом
SELECT 
    name,
    specialization,
    experience,
    rating
FROM instructors
ORDER BY rating DESC;
```

---

## Поддержка

Если возникли проблемы с подключением:
1. Проверь актуальность строки `DATABASE_URL` в настройках проекта
2. Убедись, что используешь правильный SSL режим
3. Проверь версию PostgreSQL клиента (должна быть 12+)
4. Обратись в поддержку Яндекс.Облако при проблемах с доступом

---

**Успешной работы с базой данных! 🚀**
